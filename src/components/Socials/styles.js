import styled from 'styled-components';

export const SocialsLinks = styled.article`
  display: flex;
  align-items: center;
  justify-content: space-between;

  img {
    width: 40px;
    height: 40px;
    filter: invert(1);
  }
`;
